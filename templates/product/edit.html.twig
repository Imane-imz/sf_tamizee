{% extends 'base.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
<div class="container p-5">
    <h1 class="text-center m-5">Modifier le produit</h1>

    <div>
        <div>{{ include('product/_form.html.twig', {'button_label': 'Appliquer les modifications'}) }}</div>
        <div>{{ include('product/_delete_form.html.twig') }}</div>
    </div>

    <a href="{{ path('app_product_index') }}">Retour à la liste des produits</a>
</div>
{% endblock %}
