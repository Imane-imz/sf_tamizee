{% extends 'base.html.twig' %}

{% block title %}Ajouter une ville{% endblock %}

{% block body %}

<div class="container p-5">
    <h1 class="text-center m-5">Ajouter une ville</h1>

    {{ include('city/_form.html.twig') }}

    <div class="mt-2">
        <a href="{{ path('app_city_index') }}">Retour à la liste</a>
    </div>
</div>
    
{% endblock %}
